{
  "name": "wayfinder-clients",
  "version": "0.0.0",
  "private": true,
  "description": "npm workspace root for Wayfinder's thin clients over the native Rust gateway HTTP API.",
  "workspaces": [
    "clients/shared",
    "clients/desktop"
  ],
  "scripts": {
    "parity": "npm run parity --workspace @wayfinder/shared"
  }
}
