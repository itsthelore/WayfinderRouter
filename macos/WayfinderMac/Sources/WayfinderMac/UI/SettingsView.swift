import SwiftUI

public struct SettingsView: View {
    public init() {}

    public var body: some View {
        Form {
            Text("Settings")
        }
        .padding()
        .frame(width: 360)
    }
}
