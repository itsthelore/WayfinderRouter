{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the borderless popover window. The webview reaches the gateway by direct loopback fetch (never IPC); its only Rust commands are the app-defined set in commands.rs (set_tray_state, service_control, open_target, notify), which need no plugin permission. Core window show/hide/focus is all the plugin surface it uses; the Keychain glue lands in Phase 4.",
  "windows": [
    "popover"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-show",
    "core:window:allow-hide",
    "core:window:allow-set-focus"
  ]
}
